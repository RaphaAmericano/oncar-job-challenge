{
  "name": "oncar-job-challenge",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -- turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "seed": "turbo run seed"
  },
  "devDependencies": {
    "@types/node": "^20.5.1",
    "dotenv-cli": "^7.3.0",
    "eslint-config-custom": "*",
    "prettier": "latest",
    "prisma": "^5.1.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "turbo": "latest",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "npm@8.19.2",
  "dependencies": {
    "axios": "^1.4.0"
  }
}
